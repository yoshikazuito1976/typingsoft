# Dictionary Generator Tool

このディレクトリには、タイピングソフトで使用する
ローマ字入力判定用辞書（dictionary.json）を生成する
補助ツールを配置している。

## 使い方

```bash
python3 main.py --source source.csv --out dictionary.json
```
生成された dictionary.json を本体側で使用する。